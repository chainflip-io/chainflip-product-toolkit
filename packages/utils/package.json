{
  "name": "@chainflip/utils",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepublish": "pnpm test run && pnpm build",
    "build": "tsup",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "peerDependencies": {
    "@noble/hashes": "^1.4.0",
    "zod": "^3.23.8"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    "./*": {
      "require": "./dist/*.cjs",
      "import": "./dist/*.js",
      "types": "./dist/*.d.ts"
    }
  },
  "devDependencies": {
    "@noble/hashes": "^1.4.0"
  }
}
