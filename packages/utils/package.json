{
  "name": "@chainflip/utils",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepublish": "pnpm test run && pnpm build",
    "build": "tsup",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    "./*": {
      "require": {
        "default": "./dist/*.cjs",
        "types": "./dist/*.d.cts"
      },
      "import": {
        "default": "./dist/*.js",
        "types": "./dist/*.d.ts"
      }
    }
  },
  "dependencies": {
    "@noble/hashes": "^1.4.0"
  },
  "bundledDependencies": [
    "@noble/hashes"
  ]
}
