import { fundingRedemptionExpired as vParser } from '../../100/funding/redemptionExpired';

export function fundingRedemptionExpired(): typeof vParser;
export function fundingRedemptionExpired() {
  return vParser;
}
