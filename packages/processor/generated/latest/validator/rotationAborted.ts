import { validatorRotationAborted as vParser } from '../../100/validator/rotationAborted';

export function validatorRotationAborted(): typeof vParser;
export function validatorRotationAborted() {
  return vParser;
}
