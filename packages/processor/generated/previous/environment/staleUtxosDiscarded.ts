import { environmentStaleUtxosDiscarded as vParser } from '../../141/environment/staleUtxosDiscarded';

export function environmentStaleUtxosDiscarded(): typeof vParser;
export function environmentStaleUtxosDiscarded() {
  return vParser;
}
